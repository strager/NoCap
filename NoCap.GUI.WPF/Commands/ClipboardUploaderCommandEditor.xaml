<ContentControl x:Class="NoCap.GUI.WPF.Commands.ClipboardUploaderCommandEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Content="Image uploader" Grid.Row="0" Grid.Column="0" />
        <ComboBox Grid.Column="1" Grid.Row="0" />

        <Label Content="Text uploader" Grid.Row="1" Grid.Column="0" />
        <ComboBox Grid.Column="1" Grid.Row="1" />

        <Label Content="URL shortener" Grid.Row="2" Grid.Column="0" />
        <ComboBox Grid.Column="1" Grid.Row="2" ItemsSource="{Binding Path=UrlShorteners}" DisplayMemberPath="Name" SelectedItem="{Binding Path=Command.UrlShortener}" />
    </Grid>
</ContentControl>
