<ContentControl x:Class="NoCap.Library.Editors.ImageWriterEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Name="editor">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="_Codec" />
        <ComboBox Grid.Row="0" Grid.Column="1" Name="codecSelector" SelectedItem="{Binding ElementName=editor, Path=Command.CodecInfo, Mode=TwoWay}" DisplayMemberPath="FormatDescription" ItemsSource="{Binding ElementName=editor, Path=Codecs}" />

        <Label Grid.Row="1" Grid.Column="0" Content="File _extension" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ElementName=editor, Path=Command.Extension, Mode=TwoWay}" />
    </Grid>
</ContentControl>
