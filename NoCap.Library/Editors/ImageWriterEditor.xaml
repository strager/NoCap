<ContentControl x:Class="NoCap.Library.Editors.ImageWriterEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:Controls="clr-namespace:NoCap.Library.Controls" mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Controls:CommandFactorySelector Grid.Row="0" x:Name="codecSelector"
            Command="{Binding Codec}" />

        <Controls:CommandEditor Grid.Row="1"
            Command="{Binding Codec}" />
    </Grid>
</ContentControl>
